# Experimental Modules

This directory contains prototype or exploratory modules that are not part of the production runtime.

Current content:

- `ts-agent-pipeline/src/`: TypeScript prototype of an LLM-driven precision search pipeline.

Production runtime entry points remain:

- `backend/app/main.py`
- `frontend/src/main.tsx`
- root launcher `main.py`
